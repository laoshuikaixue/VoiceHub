# LX Music / MusicFree 音源插件统一接入方案

状态：设计稿，尚未实现业务功能。探索日期：2026-09-17。

## 1. 结论与需求边界

建议在 PR #575 上改为「统一插件管理、两套协议适配器、受限执行环境、统一解析回退」。保留 MusicFree 的搜索能力，同时接入 LX 自定义音源的播放地址解析能力。

已确认的需求：

- 在后台「音源配置」直接添加多个 JS 直链，配置持久化到数据库。
- 不保留插件环境变量配置，不再以 `MUSICFREE_PLUGIN_ZIP_URL` 作为入口。
- 常驻 Node / Docker 服务保存后立即下载、验证并切换可用版本，不要求重启。
- Serverless 保存配置后，在下一次构建部署时获取并启用新脚本。
- 多个插件可启用、禁用、拖拽排序；解析失败和实际播放失败都应继续尝试后续候选。
- 涉及歌曲数据持久化、运行隔离、部署快照、备份恢复和现有播放器入口的完整接入。

本稿采用的默认行为：Serverless 中，对当前部署已经包含的插件，开关和排序在后续请求立即生效；新增脚本、修改脚本地址、协议及插件参数等待部署。此项是设计建议，尚未收到单独确认。

本期支持的是 Node.js 服务和 Node.js Serverless。不能因为 QuickJS 能运行于某些边缘平台，就直接宣布整个 VoiceHub 插件链路支持 Edge Runtime。EdgeOne 的具体运行形态需做部署验证后单独列入支持矩阵。

## 2. 探索依据

### 2.1. VoiceHub 当前实现

本地基线为 `5646ae797bfd6cce6b228bfcfdb18c08094e1451`，对应 PR #575。

| 位置 | 已确认的行为 | 新方案的处理 |
| --- | --- | --- |
| `server/utils/musicfree.ts` | 本地 JS 使用 Node VM；构建产物直接 import；缓存插件对象 | 改为统一执行宿主，第三方脚本不能在 Nitro 主线程直接 import 执行 |
| `scripts/build-musicfree-plugins.js` | 从环境变量下载 ZIP，生成 ESM 清单 | 替换为数据库配置快照及脚本数据产物生成器 |
| `app/composables/useMusicSources.ts` | MusicFree 单插件路由；结果未携带独立解析器来源 | 增加协议适配、解析器 ID、版本和候选回退结果 |
| `app/utils/musicUrl.ts` | QQ、网易云、咪咕及自配音源有既有优先级与回退分支 | 保留现有行为，统一插入插件阶段，避免新旧两条链相互递归 |
| `app/components/UI/AudioPlayer.vue` | `trySwitchPlaybackSource` 明确排除 MusicFree | 接入插件播放错误回退，并区分每个插件和版本 |
| `app/composables/usePlatformConfig.ts` | MusicFree 平台始终启用，固定排在内置平台之后 | 从后端可用能力和启用配置派生，不再绕过开关 |
| `app/drizzle/schema.ts` 的 `Song` | 仅有平台、歌曲 ID 等基础数据，没有插件原始条目 | 增加带版本的歌曲来源数据，服务器保存并恢复插件所需字段 |
| `scripts/deploy.js` / `scripts/netlify-build.js` | 数据库同步与构建并行 | 插件构建读取新表前必须完成数据库迁移 |
| `server/api/music/resolve-url.post.ts` | 现有接口主要处理 QQ，且位于匿名白名单 | 不直接把任意插件执行能力加进这个公开入口 |
| `server/api/proxy/image.get.ts` | 已有 DNS 校验、连接时校验、重定向逐跳检查 | 提取并补齐公共出站请求策略，不能直接将图片处理逻辑用于音频 |

现有 PR 的 ID 混用、插件原始字段丢失、媒体请求头未使用、本地 `btoa/atob` 反向实现，均应在此次重构覆盖的链路中修正。

### 2.2. SPlayer-Next 可参考的实现

本地参考基线为 `97cfc88243d7469b57cbd6c3571edd4bd8580ea1`。

| 文件 | 可参考内容 | 不能直接照搬的部分 |
| --- | --- | --- |
| `electron/main/plugins/lx-shim.ts` | `lx.on/send/request`、音质转换、歌曲字段转换、脚本元数据 | 桌面宿主对象注入；部分取消操作只忽略回调；`formData` 兼容不完整 |
| `electron/main/plugins/host-process.ts` | 请求 ID、generation、懒加载去重、迟到消息丢弃 | Electron `utilityProcess`、常驻心跳、单宿主承载全部插件 |
| `electron/main/plugins/host.worker.ts` | 每插件独立状态、定时器管理、卸载清理 | Node VM 不能作为服务器安全边界；共享宿主卡死会影响多个插件 |
| `electron/main/plugins/router.ts` | 调用封装、超时、请求取消、统一错误 | 需要把取消贯穿到实际 HTTP 请求和解释器任务 |
| `src/services/audioSource.ts` | 过滤候选能力，逐个尝试，返回实际 `pluginId` | 平台和 ID 映射不能假设符合 VoiceHub；不能把所有 QQ 标识互相代替 |
| `electron/main/plugins/lx-shim.test.ts` | 缺失 `hash` 不写空字符串，避免破坏 `hash ?? songmid` | 测试用例需要按 VoiceHub 数据契约重新编写 |

LX 自定义音源协议没有通用搜索接口。`kw/kg/tx/wy/mg` 是平台标识，不是独立插件 ID。MusicFree 则可提供搜索、歌词和播放地址，不能强行把两者当成同一种导出对象。

SPlayer-Next 的 LICENSE 为 AGPL-3.0，VoiceHub 为 GPL-3.0。此方案参考架构和公开协议，后续按协议独立实现；如直接移植源码，需另行处理相应许可要求。

## 3. 分层与数据流

统一划分为六层：

1. **管理配置**：数据库保存插件条目、JS URL、协议、顺序、启用状态和插件参数。
2. **脚本准备**：受控下载、大小校验、摘要、协议识别、依赖处理、生成不可变脚本产物。
3. **执行宿主**：运行限制、网络代理、定时器、取消、日志、生命周期。
4. **协议适配器**：MusicFree 对象接口与 LX 事件接口各自适配。
5. **解析调度器**：根据歌曲身份、能力、平台开关、优先级、音质和失败记录产生候选。
6. **播放器接入**：接收播放地址及解析器身份，媒体加载失败时携带已尝试列表继续解析。

常驻服务：后台保存 → 下载候选脚本 → 隔离验证 → 保存版本 → 原子切换 → 后续请求使用新版本。

Serverless：后台保存待部署配置 → 下一次构建读取数据库快照 → 下载并验证 → 打包脚本数据与清单 → 部署后按清单运行。

两条路径使用同一下载器、验证器、协议适配器和运行宿主，差异只在产物获取时机。构建产物保存的是受限环境要执行的数据，不是可直接获取服务器权限的第三方 ESM 模块。

## 4. 运行隔离选择

### 4.1. 首选 QuickJS / WASM

推荐采用 QuickJS/WASM 作为解释器，宿主只开放明确实现的能力。必要时外加 Node Worker 作为卡死回收和主线程保护手段；Worker 本身不承担权限隔离职责。

| 方案 | 判断 |
| --- | --- |
| `node:vm` / `vm + worker_threads` | 可用于上下文分离和任务终止，不能据此声称隔离了服务器权限 |
| `isolated-vm` | 可研究，但原生扩展、Node ABI、平台构建和 Serverless 部署成本更高，不作为首选 |
| QuickJS/WASM | 不直接共享 Node 对象原型；有解释器内存、栈和中断控制；更适合作为双部署形态的共同基础 |
| 独立容器执行服务 | 隔离能力更强，但增加基础设施；不作为用户启用插件的必需前提 |

不得在兼容性失败后自动退回主进程 `eval`、Node VM 或直接 import。应报告不兼容能力并跳过该插件。

### 4.2. 宿主能力边界

- 不提供 Node `process`、文件系统、数据库、宿主 `require`、子进程或 VoiceHub 环境变量。
- 不把真实 axios、Buffer、URL 类实例或宿主 Error 对象直接注入。跨边界传递受限 JSON、字符串和字节数据，在解释器内重建对象。
- 允许的 JS 依赖在解释器内部运行。HTTP 必须通过统一宿主代理，不能通过依赖包内的 Node adapter 或自带 `fetch` 绕过。
- `window/self/globalThis` 可以指向同一受限全局对象，但不提供真实 DOM、WebView、浏览器 Cookie 或 localStorage。
- `env.getUserVariables()` 只读取该插件的站点级配置。普通用户的 QQ/网易云 Cookie 不自动交给第三方插件。
- 插件定时器、挂起 Promise、HTTP 请求都归属某次初始化或调用；取消、超时、卸载时一起释放。
- 单次调用使用明确的截止时间；同步脚本、Promise jobs、异步回调进入解释器时都检查截止时间。
- 超时必须中断解释器并 Abort 出站请求。只使用 `Promise.race` 不满足要求。
- 解释器内存限制之外，还要限制宿主侧响应字节数、解压后体积、日志、并发数和二进制转换缓冲。

建议首轮验证使用 32 MiB 解释器堆、有限栈、最多 2 个并行执行槽。最终值由真实插件样本、部署函数内存和并发测试确定，不把探针结果当成生产容量结论。

实例以「插件 ID + 内容摘要 + 参数版本」为键，初始化 single-flight。同一个实例串行执行，等待队列有上限；参数或代码更新后使用新实例。常驻服务可短期保留空闲实例，Serverless 不依赖进程常驻、心跳或响应后的后台任务来保证正确性。

### 4.3. 已完成的最小可行性验证

在仓库之外的临时目录，使用 Node `v25.2.1` 与 `quickjs-emscripten@0.32.0` 运行自编样例：

- 脚本中的 `process/require/fetch` 未暴露，样例构造器访问也未取得宿主 `process`。
- 无限循环被中断处理器终止。
- 16 MiB 解释器内存限额阻止了 128 MiB 数组分配。
- MusicFree 风格 CommonJS 导出与异步 `getMediaSource` 调用通过。
- LX 风格异步初始化、注册处理器及序列化宿主 Promise 桥接通过。

这是机制可行性探针，不是安全审计，也没有验证真实第三方脚本、完整 npm 兼容层、Nuxt/Nitro 产物、Worker 打包或云端冷启动。正式实施首先完成这些验证，再锁定运行库和版本。

## 5. 两种协议的兼容契约

### 5.1. MusicFree

- 提供 `module/exports/require` 与 `env`，支持 `module.exports` 和约定的 default 导出形式。
- 搜索对接 `search(query, page, 'music')`，保留 `isEnd` 分页信息。
- 播放对接 `getMediaSource(item, quality)`，结果规范化为地址、请求头、音质和可选有效期。
- 歌词对接 `getLyric(item)`；区分 `rawLrc` 歌词正文与旧协议 `lrc` 远程地址，不能把地址字符串直接当歌词。
- 实现并测试 axios 常用请求、`create`、拦截器、`params`、二进制响应、取消和错误语义；请求始终经宿主网络代理。
- 纯 JS 依赖优先覆盖 `crypto-js/dayjs/big-integer/qs/he/cheerio`。`webdav` 需经同一受控请求层验证后开放，未实现的模块明确报错。
- 插件 `userVariables` 可生成配置表单，值按插件作用域保存并加密，不能继续统一返回空对象。
- 音质支持按协议处理 `standard/high/super/lossless`；对缺少能力声明的插件使用受控探测，不把当前 PR 的三个档位当成完整协议。

### 5.2. LX Music

- 提供 `lx`、`window.lx`、`EVENT_NAMES`、`on/send/request`、`utils`、`currentScriptInfo`、协议版本及兼容环境值。
- 必须等到脚本完成 `inited` 且注册相应处理器，才能标记 ready。顶层代码执行结束不等于初始化完成。
- 根据 `sources` 中的 `actions/qualitys` 过滤候选，保留 `qualities` 别名兼容。
- `musicUrl` 请求形状为 `{ source, action, info: { type, musicInfo } }`，标准返回值是 URL 字符串，再归一化为内部媒体结果。
- `lx.request` 保留三参数回调、原始字节、JSON 自动解析、HTTP 状态、表单与取消语义。`formData` 与 `form` 按实际协议区分，不能一律 URL 编码。
- 对 `crypto/buffer/zlib` 采用字节拷贝及有界操作，正确处理 TypedArray 的 `byteOffset/byteLength`。
- `updateAlert` 只作为更新提示，不允许脚本自动修改数据库地址或加载任意远端新代码。
- LX 在线音源通常只负责 `musicUrl`。不自动制造搜索、歌词或封面能力；支持哪些动作按实际协议版本和声明处理。

协议默认可选「自动识别 / LX / MusicFree」。自动识别只在隔离环境中验证；遇到歧义要求选择协议，不在宿主试跑。首期接受单文件 JS，不自动安装 npm 包、不支持任意相对依赖下载。

## 6. 歌曲身份与跨音源回退

必须分开三个概念：

- **站内歌曲 ID**：`Song.id`，只用于数据库记录。
- **音乐平台与外部 ID**：例如 `netease + musicId`，决定一首歌在目录中的身份。
- **解析器 ID**：具体 LX / MusicFree 插件的稳定 UUID，决定本次由谁取播放地址。

插件 ID 由数据库生成，不取文件名、URL、显示名或源码 hash。修改名称、URL、脚本版本不会改变既有歌曲的插件引用。内容 hash 只用于不可变产物和缓存校验。

### 6.1. 统一歌曲引用

内部歌曲引用包含 `catalog`、`externalId`、歌曲名、歌手、专辑、时长、已验证的平台扩展字段，以及按插件隔离的原始 item。

新增 `Song.musicSourceData`（JSONB、可空、带 `schemaVersion`）：

- 保存原始插件 ID、原始歌曲 ID、获取时的插件版本及插件 item。
- 保存必要的 `hash/strMediaMid/albumId/copyrightId` 等平台字段，区分缺失和空字符串。
- 可保存已确认的其他解析器条目映射；设置数量和总大小上限，不能无限积累搜索候选。
- 不将过期播放 URL 当成可重放身份，不把用户登录 Cookie、HTTP 认证头放入普通歌曲对象。
- 旧歌曲字段保持有效，新增字段为空时仍走原有内置音源。

搜索结果返回普通展示字段和短期、绑定调用者的加密选择凭证。投稿时服务端解开并校验凭证，再持久化原始数据，避免任意客户端随意伪造插件 item。播放已入库歌曲时由服务端根据站内 ID 加载来源数据。

原始 item 仅保存在受控服务端数据中，不加入公开歌曲列表、平台配置或普通日志。所有手写 SELECT、序列化、投稿、管理添加、导入、排期、备份和恢复入口都要审查，避免「写入了但读出来丢失」。

### 6.2. 候选间的数据转换

| 目标解析器 | 数据来源 |
| --- | --- |
| 原 MusicFree 插件 | 使用该插件自己的原始 item，并显式设置正确的外部 `id` |
| LX 对应官方平台 | 从已验证的平台字段生成 `musicInfo`，例如 `netease→wy`、`tencent→tx`、`migu→mg` |
| 另一个 MusicFree 插件 | 优先使用已有映射；否则在目标插件搜索同一歌曲，匹配成功后使用目标插件自己的 item |
| 未知目录歌曲转 LX | 需要先获得目标官方目录中的可靠身份；不能直接将其他插件 ID 当作 `songmid` |

不根据 MusicFree 的显示名猜测它一定使用哪个平台的官方 ID。需要的标准目录绑定由适配规则或明确配置声明；未经确认时按插件私有目录处理。

搜索匹配复用 `app/utils/song-name-normalize.ts` 的权威归一化，结合歌手、时长、专辑以及伴奏/翻唱/现场等版本信息。归一化同名不等于同一录音；结果有歧义、缺少足够证据或时长明显不同，应跳过该候选并继续回退，不静默替换成另一首歌。

LX 脚本声明酷狗/酷我不代表 VoiceHub 已支持它们的搜索。本期可以展示声明能力，但没有目录身份适配时不放入该歌曲候选。新增完整业务平台需另走 constants、前端平台模块、schema 默认值和自动迁移同步流程。

## 7. 数据库设计

不把所有插件塞进一个 `SystemSettings` JSON 字符串。采用独立关系表，便于稳定 ID、版本切换、排序事务和备份。

### 7.1. `MusicSourcePlugin`

| 字段 | 用途 |
| --- | --- |
| `id` UUID | 稳定插件 ID |
| `name` | 管理员显示名 |
| `enabled` | 当前启用策略 |
| `priority` | 插件列表顺序，小值优先 |
| `desiredRevision` | 管理员希望启用的配置版本 |
| `activeHotRevision/activeHotHash` | 常驻模式当前已验证版本；不用于声称某个 Serverless 部署已更新 |
| `lastValidationStatus/lastErrorCode/lastCheckedAt` | 最近验证诊断，不作为全实例运行健康状态 |
| `legacyPlatformKey` | 兼容旧 `musicfree:<文件名>` 歌曲引用，需唯一校验 |
| `createdAt/updatedAt/deletedAt` | 时间及软删除标记 |

### 7.2. `MusicSourcePluginRevision`

| 字段 | 用途 |
| --- | --- |
| `pluginId + revision` | 联合唯一键，不可变配置历史 |
| `scriptUrl` | 用户填写的 JS 直链 |
| `protocol` | `auto/lx/musicfree` |
| `userVariablesEncrypted` | 插件参数，服务端加密保存 |
| `catalogBindings` | 可选、受校验的目录身份声明 |
| `createdBy/createdAt` | 配置修改来源 |

数据库只保存 URL、配置及必要版本元数据，不保存 JS 源码。下载的源码、兼容依赖和最终产物按 SHA-256 管理，在常驻服务的受控产物目录或部署产物中保存。产物标识同时关联运行宿主版本，避免兼容层升级后误用旧编译缓存。所有生成路径均由 UUID/hash 派生，禁止使用用户文件名拼接目录。

加密优先复用经核验的站点密钥设施；若以现有站点密钥派生，应采用独立 HKDF 用途、带 key version 的 AEAD，并定义密钥轮换与备份恢复行为，不能新增插件地址环境变量。URL 查询参数可能含密钥，管理列表默认脱敏，日志不输出完整 URL。加密参数恢复到不同站点密钥时必须明确要求重新填写，或者通过专门的加密备份机制迁移，不能把无法解密的参数当空值悄悄运行。

### 7.3. `MusicSourceConfigState`

单行保存 `configRevision`，用于管理操作的乐观锁。新增、删除、拖拽排序、开关和版本选择在事务中检查并递增。排序接口提交完整插件 ID 顺序，拒绝重复、遗漏、未知 ID 或过期版本，防止两名管理员互相覆盖。

### 7.4. 版本与备份约束

- 不设置全局 `deployedVersion` 字段：同一数据库可能同时服务旧部署、新部署、预览部署和回滚部署。
- 每个部署拥有自己的 manifest；管理页面显示「期望版本」与本次部署上报的实际版本。
- 当前使用的配置历史不能在更新时删除。首期不自动清理历史配置，后续再按部署引用设计清理。
- 更新整包备份、分块恢复、自动备份和表选择 UI；按插件、配置历史、歌曲依赖关系恢复。
- 恢复旧备份时新增字段缺省，不执行备份里携带的任意源码。URL 配置恢复后仍需走下载验证/待部署流程。
- 所有迁移只由 `pnpm db:generate` 生成 SQL、snapshot 和 journal；不手写迁移文件。

## 8. 生效规则与部署一致性

### 8.1. 常驻服务即时更新

1. 保存一个新的期望版本，返回「正在验证」；操作期间禁用重复提交。
2. 受控下载脚本，计算摘要，在候选隔离实例中识别协议、等待初始化、校验能力。
3. 验证成功后将产物按 hash 写入临时文件并原子重命名；事务检查期望版本未变化，再发布 active revision/hash。
4. 后续请求使用新实例；旧实例停止接收新任务，完成或取消在途任务后释放。
5. 下载/初始化失败时保留旧版本继续服务，后台明确显示「更新失败，仍使用版本 X」。新插件则保持不可用。
6. A、B 两次更新乱序完成时，以配置版本 CAS 阻止 A 覆盖 B；删除和禁用同样检查最新策略。

单实例可使用持久卷作为产物缓存。多实例部署需要共享的不可变产物存储，或让各实例获取脚本后严格校验 active hash；若 URL 内容已改变而目标摘要无法重建，实例必须标记不可用并回退，不能悄悄运行新内容。没有共享产物存储时，无法保证远端链接变化或失效后的完整回滚与新副本可用性，页面应显示此部署能力限制。

Redis 可用于配置变更通知和共享健康状态，但不是正确性的唯一依据。解析请求读取当前配置 revision 后再选候选，不能仅依赖 pub/sub；跨实例禁用对新请求生效。在途音频流是否立即断开不作为普通开关的承诺。

### 8.2. Serverless 构建快照

构建过程：

1. 数据库迁移完成后，在 `REPEATABLE READ` 只读事务中读取插件配置与 `configRevision`，随后结束事务。
2. 在事务之外下载脚本并生成摘要，执行与常驻模式相同的隔离验证。
3. 生成不可变 manifest：顶层为 `buildId/runtimeApiVersion/configRevision`，每个插件条目为 `pluginId/pluginRevision/codeHash/capabilities`。
4. 脚本以字符串或受控资源文件打包，WASM 文件显式纳入 Nitro 产物；不得依靠运行时工作目录猜测资源路径。
5. 部署运行时只加载本次清单中的脚本。不会在普通冷启动时重新拉取 URL，也不会执行数据库刚改的新脚本。
6. 开关与排序从当前数据库策略读取；脚本、协议及插件参数仍使用清单引用的精确配置版本。插件参数不明文内联进构建产物。

构建失败不会改变旧部署实际版本。构建期间再次编辑时，本次产物仍是原快照，后台显示较新的配置尚未部署。CI 不把「构建完成」写成全站已上线状态，也不调用旧线上实例更新代码。

默认规则：启用插件的下载或验证失败，中止这次发布并报告插件名称；禁用插件可尝试打包，失败只记录不可用。启用一个当前清单没有的插件应显示「待部署」，不能显示立即可用。

同一 URL 的远端内容变更，只能通过「检查/更新」或下一次构建发现；没有实际下载前，页面不能宣称使用了远端最新版。每次构建记录真实摘要，不能只看 URL 判断版本一致。多部署滚动切换期间，管理状态响应携带 `buildId`；同一域名轮流命中不同版本时，应显示版本差异，不能让旧实例回报覆盖新实例的状态。

### 8.3. 首次部署与构建入口

- `scripts/deploy.js` 和 `scripts/netlify-build.js` 调整为先完成迁移，再执行需要读取插件表的构建；不保留两者并行导致的读表竞态。
- 直接 Serverless 构建缺少数据库或迁移时应明确失败，不能把连接故障吞成「无插件」。首次部署的新表为空则生成空清单。
- 通用 Docker/Nix 的 `node-server` 构建不依赖某个用户的数据库或私有 URL；实际服务启动并完成迁移后读取配置、获取产物。
- Docker 启动入口移除旧 `build:plugins` 下载 ZIP 流程，避免构建期和启动期来源叠加、删除的插件重新出现。
- 用解析后的 Nitro preset 生成部署能力信息；不能通过数据库是不是 Neon 来判断应用是否 Serverless。
- 预览环境必须使用对应预览数据库或明确的只读配置来源，不能由不可信 PR 构建取得生产插件参数和生产数据库权限。

## 9. 优先级与回退规则

### 9.1. 默认链路

为保持现有用户登录态和自配音源行为，内置平台默认链路为：

`既有官方/用户自配优先阶段 → 按拖拽顺序的可用插件 → 既有第三方兜底阶段`。

人工填写的固定播放地址维持现有优先行为；不能把插件生成的短期 URL 当作人工固定地址长期绕过解析。

MusicFree 私有目录歌曲在插件阶段按同一排序挑选可解析候选；非原插件需要完成第 6 节的数据匹配，不能把原始 item 无条件传给所有插件。当前原插件顺序也由管理员排序决定，但其他插件没有可靠身份时应跳过。

拖拽排序表示插件之间的优先级，不改变搜索平台 Tab 顺序。后台应分开展示「搜索平台」和「插件解析优先级」。

### 9.2. 调度规则

- 先按启用、当前部署可用版本、平台开关、动作能力、身份可转换性过滤，再按优先级顺序尝试。
- 同一请求冻结候选与版本快照；禁用/删除作为紧急策略，在实际调用前再次检查。
- 先尝试当前插件支持的目标音质；音质降级顺序有界并明示最终音质，不因高音质失败无限重试。
- 拒绝空 URL、不支持的协议、结构错误或超过大小限制的结果，继续下一候选。
- 错误类型区分：不支持、该歌曲无结果、配置错误、上游超时、运行崩溃、取消。
- 某首歌无结果不应判定整个插件离线；连续网络/运行错误才进入短期熔断，恢复探测成功后回归原顺序。
- 音源数、每次调用的出站请求数、等待队列、总截止时间均有上限。总预算不能等于「插件数量 × 各插件最大超时」。
- Serverless 将长候选链分为有界批次；必要时返回签名 continuation，携带候选快照和已尝试记录。播放器也设置整首歌的总预算，禁止无限续批。
- 实际预算由平台函数时限约束；客户端超时必须大于对应服务端批次预算，不能出现客户端先退出而服务端继续运行。

### 9.3. 实际播放失败后的回退

解析结果至少返回 `resolverId/pluginId/revision/codeHash/catalog/externalId/quality/playbackMode`。不要再统一记录成 `source: 'musicfree'`。

`<audio>` 出现网络错误、过期地址、403 或不支持的媒体时，携带已尝试的解析器版本继续请求，清除失败 URL 的缓存，不重新从同一个插件开始。保留进度，在新媒体元数据就绪后恢复；用户已经切歌时，迟到响应不得覆盖新歌曲。

每次换源与最终跳歌分开计数，保留现有 `consecutiveSkipCount` 的连续跳歌保护。用户主动暂停/切歌、Abort 和播放授权失败不能被统计成插件故障。

### 9.4. 缓存

- 缓存键包含歌曲目录、外部 ID、插件 ID、代码摘要、参数版本、音质及必要身份作用域。
- 有界 LRU/TTL，解析 Promise single-flight；失败不永久缓存，熔断状态与歌曲级无结果分开。
- 歌曲映射缓存和媒体 URL 缓存分开。插件升级后重新验证旧映射，不能仅凭相同插件 ID 永久复用。
- 开关、排序变化使候选缓存失效；命中媒体缓存前仍检查解析器当前是否允许使用。
- 不缓存或共享普通用户的官方 Cookie 到插件实例，不将有认证差异的解析结果跨用户复用。

## 10. 媒体请求头与网络策略

### 10.1. 统一出站请求代理

脚本下载、插件 API 请求、歌词远程地址和音频转发共用一套经审查的 URL/DNS 策略：

- 仅允许对应场景需要的 HTTP(S)，脚本直链默认 HTTPS；拒绝 URL 内用户名/密码和非网络协议。
- 禁止回环、私网、链路本地、云元数据、保留地址等；覆盖 IPv4、IPv6、IPv4-mapped IPv6 和特殊域名。
- 域名解析结果与实际连接一致，防止 DNS 重绑定；每次重定向重新验证，跨域时移除敏感认证头。
- 禁止插件指定底层代理、socket、DNS lookup 或连接 adapter；不能通过 axios 配置绕过策略。
- 流式限制下载及解压后的字节数、重定向次数、时间和并发；日志、错误信息及响应体截断脱敏。

现有图片代理已有可借鉴机制，但其地址分类仍需专项测试，不能不加验证直接宣称覆盖所有地址形式。

### 10.2. 播放代理

不需要特殊头且浏览器可直连的地址优先直连。需要 Referer/UA/Cookie，或 HTTPS 页面无法直接加载 HTTP 音频时，使用受控媒体代理，不能强行把 `http://` 替换成 `https://`。

- 解析服务签发短期媒体凭证，绑定歌曲、插件版本和有效期；凭证保护 URL 与敏感请求头，不接受任意 `?url=` 转发。
- 代理自身校验凭证、当前启用策略及访问权限。`<audio>` 不能携带自定义 Authorization 的场景使用专用凭证，不能把常规 JWT 放进媒体 URL。
- 支持 Range、206、Content-Range、背压、客户端断开取消；禁止整首音频先缓冲到内存。
- 多函数实例不能依赖模块级 Map 查票据：使用加密自包含凭证，或共享的有 TTL 存储。
- 歌曲正文与媒体凭证分开，公开列表不暴露上游认证头；日志不记录凭证全文。

Serverless 媒体流受平台执行时限、流式响应和带宽约束。不能承诺所有平台都能长时间代理整首音频。目标平台不满足代理要求时，将依赖该能力的候选标记为不可用并继续回退；完整支持这类音源需要经过验证的流媒体转发服务。此项在验收中单独列出，不能只用「取到了 URL」判定成功。

## 11. 管理页面与 API

扩展现有 `MusicSourceController.vue`，保留内置平台控制，新增「插件音源」区域。

每行显示：拖拽手柄、名称、LX/MusicFree 标识、支持的平台/动作、启用开关、实际版本、待部署/更新失败状态及操作菜单。URL 默认脱敏；错误详情在展开区域显示，长列表内部滚动。

新增/编辑表单包括名称、JS 直链、协议，以及插件声明的参数。使用 `CustomSelect`、`Teleport` 模态框、`AppSpinner`、语义主题颜色和双语词典，不另起一套 UI 模式。

- 常驻服务按钮使用「保存并加载」；显示验证过程，成功后立即可用，失败时说明旧版是否仍在服务。
- Serverless 使用「保存，部署后生效」，明确显示当前部署版本与待部署配置。
- 同一 URL 更新内容时提供「重新检查/更新」，不要求伪造 URL 或改插件名称。
- 开关与拖拽结束自动保存；保存期间禁用重复操作，失败恢复 UI，冲突刷新最新排序。
- 提供管理员测试解析入口，显示依次尝试的插件、耗时、跳过原因和命中来源，不输出 Cookie、脚本源码或完整敏感 URL。

建议接口：

| 接口 | 用途 |
| --- | --- |
| `GET /api/admin/music-source-plugins` | 管理列表、部署能力、期望与实际版本 |
| `POST /api/admin/music-source-plugins` | 新增脚本配置 |
| `PUT /api/admin/music-source-plugins/:id` | 新配置版本与参数 |
| `PATCH /api/admin/music-source-plugins/:id/enabled` | 即时开关 |
| `PUT /api/admin/music-source-plugins/order` | 事务保存完整顺序及 revision |
| `POST /api/admin/music-source-plugins/:id/refresh` | 常驻更新；Serverless 标记下次构建刷新 |
| `POST /api/admin/music-source-plugins/:id/test` | 有限预算的验证/测试，不能切换线上快照 |
| `DELETE /api/admin/music-source-plugins/:id` | 软删除并立即阻止后续调用 |
| `GET /api/music-source-plugins/capabilities` | 脱敏的可用平台、动作和显示名称 |
| `POST /api/music-source-plugins/search` | MusicFree 搜索及选择凭证 |
| `POST /api/music-source-plugins/resolve` | 统一插件解析与有界回退 |
| `POST /api/music-source-plugins/lyric` | 对应歌曲的插件歌词 |
| `GET /api/music-source-plugins/media` | 验证媒体凭证并流式转发 |

脚本管理权限建议限定为 `SUPER_ADMIN`；普通音源展示可向相应管理员开放。调用接口要求现有登录态或已有受限设备/API Key 权限，并叠加账号、IP、插件及全局并发限额。不能因合并到旧公开解析接口而绕过这些限制。

匿名播放如需保留，只允许对符合站点公开策略的已入库歌曲签发有限调用凭证，不开放任意脚本 ID、任意原始 item 或任意 URL 执行。

所有新增业务错误使用 `createApiError + SERVER_ERROR_CODES`，中文/英文同步，客户端统一 `useServerErrors().localize`。

## 12. 改动范围与旧版迁移

预计新增的实现目录职责：

- `server/utils/music-source-plugins/`：配置、版本、产物、宿主、网络、协议适配、歌曲引用、调度、媒体凭证。
- `server/api/admin/music-source-plugins/`：配置管理和诊断。
- `server/api/music-source-plugins/`：搜索、解析、歌词和媒体代理。
- `scripts/build-music-source-plugins.*`：读取构建配置快照、生成受控产物清单。
- 前端共享平台/插件类型及 composable，复用既有管理员页面与播放器入口。

必须同时梳理：搜索试听、投稿后播放、歌曲列表、排期列表、自动连播、音质切换、下载、备用音源切换、歌词、开放 API 和设备控制。搜索与播放调用应收敛到同一插件服务，不能只修改点歌页面。

旧实现处理：

- 删除环境变量 ZIP 配置与旧构建分支，更新 `.env.example`、Dockerfile、README 和部署脚本。
- 不继续同时加载「目录插件 + ZIP 产物 + 新数据库插件」，避免已删除插件从旧来源复活。
- 旧 `musicfree:<文件名>` 通过显式 legacy alias 关联到稳定插件 UUID；有旧歌曲但缺少原始 item 时尝试安全重建，无法重建则清楚报错。
- 不伪造 ZIP 中单个 JS 的公网直链。原 ZIP 用户需要为各脚本提供实际 JS URL；过渡说明列出此要求。
- 旧 MusicFree API 可暂留为受相同权限、开关和预算约束的薄转发，不能继续保有第二套执行逻辑。
- MusicFree 独立搜索目录保留；LX 作为解析提供者，不制造不存在的搜索 Tab。
- 新增/删除实现文件后同步 README「项目结构」。涉及 SystemSettings 新字段时执行仓库完整同步清单；本方案优先独立表，不增加重复的全局配置字段。

## 13. 实施顺序

### 阶段 A：运行机制与部署验证

在隔离样例和经选择的真实插件上验证 LX 初始化、MusicFree 常用依赖、二进制数据、参数、取消、循环中断和内存清理。生成真实 Nitro Node / Vercel / Netlify 产物，检查 WASM 和 Worker 资源路径、冷启动、超时回收。失败时修正适配器或缩小明确支持的能力，不退回不安全执行模式。

### 阶段 B：配置与版本基础

新增数据库表和歌曲来源字段，通过工具生成迁移；实现管理 API、配置事务、受控下载、常驻切换、Serverless 快照、错误码和备份恢复。

### 阶段 C：双协议和解析调度

完成 LX / MusicFree 适配，歌曲 ID 转换、原始 item 保存、匹配规则、排序回退、缓存和限流；将内置解析的前置与兜底阶段明确拆开，避免重复调用。

### 阶段 D：页面及播放全过程

完成管理页面、拖拽/开关自动保存、版本状态、诊断；覆盖试听、入库重放、排期、下载、媒体请求头和播放器失败回退。

### 阶段 E：迁移、验证与文档

移除旧环境变量/ZIP 入口，验证旧歌曲 alias、部署回滚、备份恢复、两种语言和三种主题，更新部署与插件兼容说明。

这些阶段共同组成此次功能，不把歌曲持久化、播放失败回退或 Serverless 快照留成「以后再做」。可以按阶段形成可审查提交，但只有验收矩阵通过后才宣布完整支持。

## 14. 验收矩阵

| 场景 | 必须观察到的结果 |
| --- | --- |
| 同时配置 LX A、MusicFree B、LX C | 三者共存，按能力筛选并遵守保存后的顺序 |
| A 超时 / 无 URL / 不支持音质 | 取消相关任务后尝试后续候选，错误原因可定位 |
| A 返回 URL 但浏览器 403 / 解码失败 | 跳过 A 的对应版本，继续回退；不会立刻把整首歌跳过 |
| 切歌时旧解析完成 | 旧结果不覆盖新歌曲；取消不计入音源故障 |
| MusicFree 搜索后投稿、刷新、次日排期播放 | 使用正确外部 ID 和完整插件字段，不使用数据库 ID |
| 不同目录存在同名翻唱/伴奏 | 不静默匹配错误版本；无可靠匹配时继续下一候选 |
| 同时两次更新、删除与更新竞态 | 旧任务不能覆盖最新配置或复活已删除插件 |
| 常驻更新下载失败/初始化失败 | 老版本继续服务，新配置有明确失败状态 |
| Serverless 修改 URL | 老部署继续使用自身清单，后台显示待部署 |
| Serverless 构建失败、构建中再编辑、回滚 | 各部署版本可区分，不能把期望版本误报为已上线 |
| Serverless 修改顺序、禁用已部署插件 | 后续请求遵守最新策略，缓存不绕过开关 |
| 冷启动、多实例、无 Redis | 行为正确；共享限流降级能力明确，不依赖常驻定时器 |
| 脚本尝试循环、超额内存、过量日志 | 请求有界失败，主服务可继续响应 |
| 私网 URL、重定向私网、DNS 重绑定、压缩炸弹 | 在统一网络层拒绝，不触达数据库/云元数据等内部服务 |
| 特殊头音频、Range、拖动进度、用户断开 | 代理头正确，206 行为正确，任务及时取消 |
| 源码/URL/插件参数含敏感信息 | 普通 API、公开页面和日志不泄漏 |
| 导出及整包/分块恢复 | 插件配置、历史引用和歌曲来源数据完整；旧备份可恢复 |
| 中文/英文、三主题、长名称和移动端 | 配置行、状态、模态框和排序操作均可用 |

尚未验证：真实音源脚本兼容覆盖、生产流媒体代理成本、目标 Serverless 平台完整部署。上述内容应作为实现验收门槛，不能用本地样例或构建成功替代。

## 15. 外部协议与运行库参考

- LX 源码基线：<https://github.com/lyswhut/lx-music-desktop/tree/950029884c063bec6847402c681c89678ea48d24>。
- MusicFree 插件接口：<https://github.com/maotoumao/MusicFree/blob/master/src/types/plugin.d.ts>。
- SPlayer-Next：<https://github.com/SPlayer-Dev/SPlayer-Next>，具体参考文件见第 2 节。
- QuickJS/WASM：<https://github.com/justjake/quickjs-emscripten>。运行库需要持续更新和专项测试；引入解释器不等于无需审查宿主桥接与网络策略。
