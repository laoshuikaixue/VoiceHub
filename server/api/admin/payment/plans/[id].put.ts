import { eq } from 'drizzle-orm'; import { db } from '~/drizzle/db'; import { paymentPlans } from '~/drizzle/schema'; import { requirePaymentAdmin } from '~~/server/utils/paymentAuth'; import { createApiError } from '~~/server/utils/apiError'; import { SERVER_ERROR_CODES } from '~~/server/config/constants'; import { getServerDate } from '~~/server/utils/serverTime'
export default defineEventHandler(async event => { requirePaymentAdmin(event); const id = Number(getRouterParam(event, 'id')); const body = await readBody(event); const currency = String(body?.currency || 'CNY').trim().toUpperCase(); const validityUnit = String(body?.validityUnit || 'day'); const validityValue = Number(body?.validityValue ?? 30); const priceCents = Number(body?.priceCents); const cardCount = Number(body?.cardCount); const originalPriceCents = body?.originalPriceCents === null || body?.originalPriceCents === undefined ? null : Number(body.originalPriceCents); if (!Number.isInteger(id) || !body?.name || !Number.isInteger(priceCents) || priceCents < 1 || (originalPriceCents !== null && (!Number.isInteger(originalPriceCents) || originalPriceCents < 0)) || !Number.isInteger(cardCount) || cardCount < 1 || !Number.isInteger(validityValue) || validityValue < 1 || !/^[A-Z]{3}$/.test(currency) || !['day', 'month', 'year'].includes(validityUnit)) throw createApiError(400, SERVER_ERROR_CODES.COMMON_INVALID_PARAMS, '套餐参数无效'); const [updated] = await db.update(paymentPlans).set({ name: String(body.name).trim().slice(0, 100), description: String(body.description || ''), priceCents, originalPriceCents, currency, cardCount, validityValue, validityUnit, features: Array.isArray(body.features) ? body.features.map(String).slice(0, 20) : [], forSale: body.forSale !== false, sortOrder: Number(body.sortOrder || 0), updatedAt: getServerDate() }).where(eq(paymentPlans.id, id)).returning(); return updated })
