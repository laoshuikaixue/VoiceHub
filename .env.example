# 数据库连接地址（必填）
DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require"

# JWT认证密钥（必填）
JWT_SECRET="your-very-secure-jwt-secret-key"

# 应用运行环境（可选）
NODE_ENV=development

# Prisma日志级别（可选，默认为error）
# 可选值：error, warn, info, query
# error: 只显示错误日志
# warn: 显示错误和警告日志
# info: 显示错误、警告和信息日志
# query: 显示所有日志包括SQL查询（开发调试用）
PRISMA_LOG_LEVEL=error

# Redis缓存服务（可选，用于提升性能）
# 如果设置了REDIS_URL，系统将自动启用Redis缓存功能
# REDIS_URL="redis://default:password@host:port"

# 部署平台预设（可选，部署时自动设置）
# NITRO_PRESET=vercel

# 数据库连接池空闲模式控制（可选，默认为true）
# 控制是否启用数据库连接的空闲模式功能
# true: 启用空闲模式，适用于托管数据库服务（如Neon）
#       系统会在空闲时自动调整连接数，节省资源和成本
# false: 禁用空闲模式，适用于自建PostgreSQL数据库
#        保持持久连接，确保系统稳定性和响应速度
# 推荐设置：
# - 托管数据库服务：保持默认值true
# - 自建PostgreSQL：设置为false
ENABLE_IDLE_MODE=true