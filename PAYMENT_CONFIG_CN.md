# VoiceHub 支付系统配置指南

VoiceHub 内置支付系统，用户可以直接购买点歌券套餐，无需单独部署支付站点。本文按照管理后台的配置顺序说明支付设置、服务商实例、回调地址、订单处理和故障排查。

## 目录

- [支持的支付方式](#支持的支付方式)
- [快速开始](#快速开始)
- [全局支付设置](#全局支付设置)
- [支付策略与渠道路由](#支付策略与渠道路由)
- [服务商配置](#服务商配置)
- [服务商实例管理](#服务商实例管理)
- [Webhook 配置](#webhook-配置)
- [支付流程与订单状态](#支付流程与订单状态)
- [退款流程](#退款流程)
- [常见问题](#常见问题)

## 支持的支付方式

| 服务商类型 | 支持方式 | 适用场景 |
| --- | --- | --- |
| **EasyPay（易支付）** | 支付宝、微信支付 | 兼容易支付协议的第三方聚合支付 |
| **支付宝官方** | 支付宝二维码、移动端跳转/唤起 | 直接对接支付宝开放平台 |
| **微信支付官方** | 微信扫码 | 直接对接微信支付 APIv3 |
| **Stripe** | 银行卡及 Stripe 账户支持的方式 | 国际支付、多币种场景 |
| **Airwallex** | 国际卡支付及 Airwallex 支持的方式 | 跨境收款 |

同一种支付方式可以配置多个服务商实例。用户端显示的是支付宝、微信支付、Stripe 或 Airwallex 等支付方式，后台再根据服务商的启用状态、支持方式、排序和限额选择实际通道。多个服务商不会因为支持同一方式而互相强制关闭，可以分别启用或禁用。

## 快速开始

1. 使用管理员账号进入 **支付管理 → 支付设置**。
2. 打开 **启用支付**。
3. 设置商品名前缀、货币、金额范围、订单超时时间和最大待支付订单数。
4. 在页面底部 **服务商管理** 中添加至少一个服务商实例。
5. 填写服务商凭证和支持的支付方式，保存服务商。
6. 将系统生成的异步通知地址配置到支付平台后台。
7. 创建一笔小额测试订单，确认支付、回调、发券和查单均正常。

## 全局支付设置

### 商品与金额

| 设置项 | 说明 | 默认/建议 |
| --- | --- | --- |
| **商品名前缀** | 支付页面显示的套餐名前缀 | 例如 `VoiceHub 点歌券` |
| **货币类型** | 套餐和支付订单使用的默认货币 | `CNY` |
| **商品名后缀** | 系统会使用货币代码作为商品名后缀 | 例如 `CNY`、`USD` |
| **最低金额** | 单笔订单最低金额，单位为元 | 默认 `1` |
| **最高金额** | 单笔订单最高金额，单位为元 | 留空表示不限制 |
| **每日限额** | 用户每日累计支付上限，单位为元 | 留空表示不限制 |
| **订单超时时间** | 待支付订单超过该分钟数后进入过期处理 | 至少 1 分钟 |
| **最大待支付订单数** | 同一用户同时存在的待支付订单数量 | 默认 `3` |

管理界面中的金额输入均以“元”显示；接口和数据库中的金额字段使用“分”保存。例如 `1.50` 元会保存为 `150` 分。套餐价格也应使用元填写，系统会自动换算为分。

### 取消频率限制

该设置用于限制用户频繁创建并取消订单：

| 设置项 | 说明 |
| --- | --- |
| **启用限制** | 是否限制取消频率 |
| **窗口模式** | 滚动窗口或固定窗口 |
| **窗口长度** | 统计取消次数的时间长度 |
| **窗口单位** | 分钟、小时或天 |
| **最大次数** | 窗口内允许的最大取消次数 |

### 支付策略

| 设置项 | 说明 |
| --- | --- |
| **负载均衡策略** | `轮询`按服务商顺序分配；`最少金额`优先选择当日累计金额较少的服务商 |
| **支付宝强制二维码支付** | 让支付宝订单优先返回二维码，不跳转手机网站 |
| **支付宝移动端当面付唤起** | 移动端优先尝试唤起支付宝 App，失败时显示二维码备用页 |
| **帮助图片** | 用户支付页展示的客服二维码或说明图片 |
| **帮助文本** | 用户支付页展示的支付说明 |

## 支付策略与渠道路由

服务商列表中的“启用”只控制该服务商实例是否可以接单；“支持的支付方式”控制它能处理哪些方式；列表顺序决定同策略下的优先级。

例如同时配置一个 EasyPay 和一个支付宝官方服务商：

- 两个服务商都可以保存为启用状态。
- 用户选择支付宝时，系统只会从支持支付宝且已启用的实例中选择。
- 如果优先实例达到限额、配置不完整或请求失败，系统会按策略尝试其他可用实例。
- 禁用某个实例只会将它从候选列表移除，不会影响其他支付宝服务商。

服务商优先级可以在管理列表中拖动调整。拖动完成后系统会保存 `sortOrder`，刷新页面后仍保持新顺序。

## 服务商配置

进入 **服务商管理 → 添加服务商**，先选择类型，再填写名称、排序、支付方式和凭证。异步通知地址与同步跳转地址由系统根据当前站点域名生成，不需要手工拼接。

### EasyPay（易支付）

EasyPay 适用于兼容易支付协议的聚合平台。

| 参数 | 说明 | 必填 |
| --- | --- | --- |
| **商户 PID** | 易支付商户 ID | 是 |
| **商户密钥 PKey** | 易支付签名密钥 | 是 |
| **API 基础地址** | 易支付站点根地址，例如 `https://pay.example.com` | 是 |
| **通用渠道 ID** | 所有方式共用的渠道 ID | 否 |
| **支付宝渠道 ID** | 指定支付宝渠道 | 否 |
| **微信渠道 ID** | 指定微信渠道 | 否 |

API 基础地址不要附加 `submit.php`。支付模式可以选择二维码或弹窗。当前适配器按易支付常见 V2 下单、查单和通知协议处理；如果平台只提供旧 V1 协议，请确认它是否兼容 V2 的签名、字段和返回值，否则不能直接保证兼容。

### 支付宝官方

| 参数 | 说明 | 必填 |
| --- | --- | --- |
| **应用 App ID** | 支付宝开放平台应用 ID | 是 |
| **应用私钥** | RSA2 应用私钥，建议完整粘贴 PEM 内容 | 是 |
| **支付宝公钥** | 支付宝开放平台生成的公钥 | 是 |
| **支付宝网关** | 正式或沙箱网关地址 | 否 |

正式环境通常使用 `https://openapi.alipay.com/gateway.do`。移动端可选择手机网站跳转或当面付二维码/唤起；桌面端优先使用二维码。启用当面付前，需要在支付宝开放平台为应用开通对应产品权限。

### 微信支付官方

| 参数 | 说明 | 必填 |
| --- | --- | --- |
| **应用 App ID** | 微信开放平台或公众号 App ID | 是 |
| **商户号** | 微信支付商户号 | 是 |
| **商户 API 私钥** | PEM 格式商户私钥 | 是 |
| **API v3 密钥** | 32 位 API v3 密钥 | 是 |
| **微信支付公钥** | 微信支付公钥 PEM 内容 | 是 |
| **微信支付公钥 ID** | 微信支付公钥 ID | 是 |
| **商户证书序列号** | 商户证书序列号 | 是 |
| **退款通知地址** | 独立退款通知地址 | 否 |

请确保 API v3 密钥、私钥、公钥和证书序列号属于同一个商户环境。测试环境和生产环境的凭证不能混用。

### Stripe

| 参数 | 说明 | 必填 |
| --- | --- | --- |
| **Secret Key** | `sk_test_...` 或 `sk_live_...` | 是 |
| **Publishable Key** | `pk_test_...` 或 `pk_live_...` | 是 |
| **Webhook Secret** | Stripe Webhook 签名密钥 `whsec_...` | 是 |
| **支付币种** | 与套餐和 Stripe 账户一致的币种 | 是 |

测试时使用 test 模式的三组密钥；切换生产必须同时替换 Secret Key、Publishable Key、Webhook Secret，并确认币种和账户权限。

### Airwallex

| 参数 | 说明 | 必填 |
| --- | --- | --- |
| **Client ID** | Airwallex 客户端 ID | 是 |
| **API Key** | API 密钥 | 是 |
| **Webhook Secret** | Webhook 签名密钥 | 是 |
| **API 基础地址** | 生产或 Demo API 地址 | 是 |
| **国家/地区代码** | 例如 `CN` | 是 |
| **支付币种** | 订单使用的币种 | 是 |
| **Airwallex 账户 ID** | Scoped API Key 对应账户 ID | 否 |

生产环境通常使用 `https://api.airwallex.com/api/v1`，测试密钥使用 Airwallex Demo API 地址。API 地址和密钥环境必须匹配。

## 服务商实例管理

同一种服务商可以创建多个实例，用于多通道、分流和故障切换。每个实例均可独立设置：

- **启用/禁用**：单独控制实例是否接单，不会影响同渠道的其他实例。
- **支付方式**：只选择该实例实际开通的方式。
- **退款开关**：独立允许或禁止该实例退款。
- **用户退款**：只有开启退款后才能允许用户申请退款。
- **优先级**：拖动列表调整，顺序保存到数据库。
- **按支付方式限额**：为每个实例的每种支付方式设置单笔最低、单笔最高和每日限额。
- **删除**：删除前会弹出确认，删除后该实例不再参与新订单选择。

限额单位为元，留空或填写 `0` 表示不限制。系统选择通道时会跳过已禁用、支付方式不匹配、超过单笔限额或达到每日限额的实例。

## Webhook 配置

添加服务商时系统会显示当前域名对应的异步通知地址和同步跳转地址。异步通知地址必须配置到服务商后台，支付完成后系统依靠它更新订单并发放点歌券。

回调地址格式为：

```text
https://你的域名/api/payment/webhook/{providerKey}
```

常见路径如下：

| 服务商 | `providerKey` | 回调地址示例 |
| --- | --- | --- |
| EasyPay | `easypay` | `/api/payment/webhook/easypay` |
| 支付宝官方 | `alipay` | `/api/payment/webhook/alipay` |
| 微信支付官方 | `wxpay` | `/api/payment/webhook/wxpay` |
| Stripe | `stripe` | `/api/payment/webhook/stripe` |
| Airwallex | `airwallex` | `/api/payment/webhook/airwallex` |

### Webhook 检查清单

1. 使用公网 HTTPS 域名，不要填写本地 IP 或 `localhost`。
2. 在 EdgeOne、Vercel、Netlify、Cloudflare Pages 等托管或反向代理环境中，建议设置 `NUXT_PUBLIC_HOST=https://你的公网域名`。系统也会识别平台提供的生产域名变量（如 `VERCEL_PROJECT_PRODUCTION_URL`、`URL`、`CF_PAGES_URL`），避免把内部 endpoint 或临时部署域名写入回调地址。
3. 服务商后台的通知地址必须与管理界面显示的地址完全一致。
4. 防火墙、反向代理和 WAF 需要允许支付平台的 POST 请求。
5. Stripe 和 Airwallex 的 Webhook Secret 必须填写对应端点生成的密钥。
6. 修改域名后要重新核对所有服务商回调地址。
7. 回调重复到达是正常网络行为，系统应以订单状态和上游订单号幂等处理，不会重复发券。

## 支付流程与订单状态

```text
用户选择套餐和支付方式
        ↓
创建 PENDING 订单
  校验金额、每日限额、待支付订单数
  按策略选择可用服务商实例
        ↓
返回二维码、跳转地址或客户端支付信息
        ↓
用户完成支付
        ↓
服务商 Webhook 验签并更新订单
        ↓
发放点歌券，订单变为 COMPLETED
```

| 状态 | 说明 |
| --- | --- |
| `PENDING` | 待支付，等待用户完成支付 |
| `PAID` | 已确认支付，等待完成发券 |
| `COMPLETED` | 已完成，点歌券已发放 |
| `EXPIRED` | 超时未支付，订单已过期 |
| `CANCELLED` | 用户主动取消 |
| `FAILED` | 支付或发券失败，可在后台重试查单 |
| `REFUND_REQUESTED` | 已提交退款申请 |
| `REFUNDING` | 退款处理中 |
| `REFUNDED` | 退款完成 |

订单超时后后台任务会先向上游查单，再决定是否标记为过期。用户已付款但回调延迟时，管理员可在订单管理中执行“重试/查单”，避免因为回调延迟重复下单。

## 退款流程

1. 在服务商实例中启用“允许退款”。
2. 如需用户自助申请退款，再启用“允许用户申请退款”。
3. 确认服务商账号具备退款权限，并已配置退款所需证书或密钥。
4. 管理员在订单管理中选择符合条件的订单发起退款。
5. 系统向对应订单的原服务商提交退款，并根据退款通知更新状态。

退款能力受服务商、订单状态、退款金额和支付平台权限共同限制。不要在支付平台实际退款成功前手动把订单改为已退款。

## 验证清单

- 支付设置已启用，最低金额、货币和套餐币种一致。
- 服务商名称、类型、凭证和支付方式填写正确，状态为已启用。
- 服务商排序和限额符合实际通道优先级。
- 用户支付页展示的方式与已启用服务商一致。
- 小额订单可以成功创建并进入支付页面。
- Webhook 到达后订单变为已完成，点歌券只发放一次。
- 后台订单重试查单可以正确同步上游状态。
- 退款测试订单能进入退款中并最终变为已退款。

## 常见问题

### 支付页没有支付方式

检查全局“启用支付”、服务商是否启用、服务商是否选择了对应支付方式、服务商凭证是否填写完整，以及服务商限额是否把当前金额排除。

### 订单一直待支付

检查异步通知地址、HTTPS、反向代理和服务商后台回调配置。不要只依赖同步跳转地址，订单最终状态以 Webhook 或后台查单为准。

### 查询订单返回错误

确认服务商 API 地址、商户密钥、币种、商户权限和接口版本。EasyPay 平台尤其要确认使用的是兼容 V2 签名和查单返回格式的接口。

### 保存后服务商仍不可用

检查必填字段是否完整、支付方式是否至少选择一项、实例是否被禁用，以及单笔/每日限额是否已达到。服务商列表中的禁用状态不会自动恢复，需要手动重新启用。

### 回调验签失败

确认公钥、私钥、证书序列号、API v3 密钥或 Webhook Secret 属于同一环境；复制 PEM 密钥时不要遗漏头尾行或换行内容。

## 安全建议

- 不要把支付密钥提交到 Git、截图、聊天记录或公开日志。
- 生产环境和测试环境使用不同的服务商实例和密钥。
- 定期轮换密钥，并及时删除不再使用的实例。
- 回调地址使用 HTTPS，并限制支付平台后台的 API 和通知权限。
- 先用小额订单验证完整链路，再开放给全部用户。
