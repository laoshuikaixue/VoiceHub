{
  "buildCommand": "node scripts/deploy.js",
  "installCommand": "pnpm install --frozen-lockfile || pnpm install",
  "functions": {
    "server/api/open/backup/auto.post.ts": {
      "maxDuration": 60
    }
  },
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "no-cache, no-store, must-revalidate"
        }
      ]
    }
  ]
}
